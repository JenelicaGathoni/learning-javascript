/* for (triangle = "#"; triangle.length < 7; triangle.length + "#/n") {
  console.log(triangle);
} */
/* let triangle = "#";
while (triangle.length < 8) {
  console.log(triangle);
  triangle + "#/n";
} */
let triangle = "#";
while (triangle.length < 8) {
  console.log(triangle);
  triangle += "#";
}
