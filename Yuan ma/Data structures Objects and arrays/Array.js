let numbers = [2, 3, 5, 6, 11];
console.log(numbers[2]);
// there are two main ways to access values in javascript
// Using square brackets and dot notation
// when using a dot the word after the dot is the literal name of the property.
// when using square brackets the expression between the brackets is evaluated to get the property name.
// the dot notation only works with names that look like a valid variable name.
//
