//Define the function min that takes two arguments and returns their minimum.

const kidogo = function (no1, no2) {
  if (no1 > no2) {
    return no2;
  } else {
    return no1;
  }
};
console.log(kidogo(0, 10));
